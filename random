term = conf.get("term")
if term is not None:
    term_str = str(term).strip()
    # If CU returned just a number, append "years"
    if term_str.isdigit():
        term = f"{term_str} years"
    elif term_str.replace('.','',1).isdigit():
        term = f"{term_str} years"
story += field_row("Confidentiality Term", term, styles, provenance)
